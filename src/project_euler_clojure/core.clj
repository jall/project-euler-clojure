(ns project-euler-clojure.core
  (:gen-class)
  (:require [clojure.math.numeric-tower :refer :all]
            [clojure.math.combinatorics :as combinatorics]))

(defn -main
  [& args]
  (println "Run one of the individual questions instead"))
